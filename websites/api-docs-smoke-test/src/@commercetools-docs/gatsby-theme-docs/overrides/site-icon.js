import React from 'react';
import SiteIcon from '../../../icons/HttpApiSmall';

// A React component to be rendered next to the microsite title
// eslint-disable-next-line react/display-name
export default () => {
  return (
    <div>
      <SiteIcon />
    </div>
  );
};
