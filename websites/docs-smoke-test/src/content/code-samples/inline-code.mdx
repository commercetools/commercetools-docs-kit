---
title: Markdown and Content Smoketest Page
shortTitle: Inline Code
---

This text paragraph contains various `code` text spans inline in the lines but does not know about the programming language.
They have no language marking but `they can be very long and have so much text that could maybe not fit into a signle line so we have to handle line wrapping inside`. This is nice.
