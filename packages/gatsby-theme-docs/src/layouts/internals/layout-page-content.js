import styled from '@emotion/styled';
import { GRID_ID_PAGE_CONTENT } from './layout-design-config';

const LayoutPageContent = styled.div`
  grid-area: ${GRID_ID_PAGE_CONTENT};
`;

export default LayoutPageContent;
