import React from 'react';
import PropTypes from 'prop-types';

const VideoPlayer = () => {
  return <div>Client Site VideoPlayer</div>;
};
// VideoPlayer.propTypes = {
//   video: PropTypes.string.isRequired,
// };

export default VideoPlayer;
